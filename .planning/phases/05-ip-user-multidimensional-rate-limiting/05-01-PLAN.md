---
phase: 05-ip-user-multidimensional-rate-limiting
plan: 01
type: tdd
wave: 1
depends_on:
  - 04-03
files_modified:
  - tests/Unit/SudoSessionTest.php
  - includes/class-sudo-session.php
autonomous: true

must_haves:
  truths:
    - "Per-IP failed-attempt tracking is implemented in Sudo_Session"
    - "Combined lockout triggers when either user attempts or IP attempts reaches threshold"
    - "attempt_activation returns locked_out during active IP lockout"
    - "wp_sudo_lockout includes request IP as an additive third argument"
    - "Existing result-code contract stays unchanged"
  artifacts:
    - path: "includes/class-sudo-session.php"
      provides: "IP tracking + combined lockout policy"
      contains: "record_failed_attempt"
    - path: "tests/Unit/SudoSessionTest.php"
      provides: "Unit coverage for IP + user multidimensional lockout"
      contains: "ip"
  key_links:
    - from: "Sudo_Session::record_failed_attempt()"
      to: "user meta + transient state"
      via: "dual-write failure tracking"
      pattern: "lockout"
    - from: "Sudo_Session::attempt_activation()"
      to: "IP lockout gate"
      via: "pre-password lockout evaluation"
      pattern: "locked_out"
---

<objective>
Implement the core v2.13 multidimensional rate-limit behavior in `Sudo_Session` with strict unit-first TDD.

Purpose: add IP tracking and combined lockout without regressing existing challenge/session contracts.
Output: passing unit tests and minimal production changes.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/ROADMAP.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/05-ip-user-multidimensional-rate-limiting/05-CONTEXT.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/05-ip-user-multidimensional-rate-limiting/05-RESEARCH.md
@/Users/danknauss/Documents/GitHub/wp-sudo/includes/class-sudo-session.php
@/Users/danknauss/Documents/GitHub/wp-sudo/tests/Unit/SudoSessionTest.php
</context>

<tasks>

<task type="tdd">
  <name>Task 1 RED: Add failing unit tests for IP tracking and combined lockout</name>
  <files>tests/Unit/SudoSessionTest.php</files>
  <action>
Add failing unit tests that define the new behavior before implementation.

Required coverage:
1. Request IP extraction defaults to sanitized `REMOTE_ADDR` and falls back safely.
2. `record_failed_attempt()` updates IP tracking and triggers lockout when IP threshold is reached first.
3. `attempt_activation()` returns `locked_out` during active IP lockout, before password verification.
4. `wp_sudo_lockout` hook payload preserves `(user_id, attempts)` and adds third arg `ip`.

Run focused tests (expect RED):
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit -- tests/Unit/SudoSessionTest.php --filter "ip|lockout|failed_attempt" --do-not-cache-result
```

Commit: `test(05-01): add failing unit tests for ip user multidimensional rate limiting`
  </action>
  <verify>
New unit tests fail for expected reasons against pre-change production code.
  </verify>
  <done>RED baseline exists for v2.13 core behavior.</done>
</task>

<task type="tdd">
  <name>Task 2 GREEN: Implement minimal IP + user combined lockout logic</name>
  <files>includes/class-sudo-session.php</files>
  <action>
Implement the smallest code changes needed to satisfy Task 1.

Implementation requirements:
1. Add per-IP failed-attempt storage and pruning.
2. Add active IP lockout tracking and remaining-time helper.
3. Update lockout checks in `attempt_activation()` to include IP lockout state.
4. Update `record_failed_attempt()` to trigger lockout when either user or IP attempts reaches threshold.
5. Fire `wp_sudo_lockout` with third argument `ip` while preserving first two args.

Run verification after this implementation step:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit && composer analyse:phpstan
```

Commit: `feat(05-01): implement ip user multidimensional rate limiting core`
  </action>
  <verify>
Unit suite and PHPStan both pass after implementation.
  </verify>
  <done>Core multidimensional logic is implemented and green at unit level.</done>
</task>

<task type="tdd">
  <name>Task 3 REFACTOR: Tighten helper structure and docs-in-code</name>
  <files>includes/class-sudo-session.php tests/Unit/SudoSessionTest.php</files>
  <action>
Refactor without behavior change:
- keep helper naming and constants consistent,
- clarify docblocks for new IP storage/lockout paths,
- remove duplicated time/remaining calculations where appropriate.

Run verification after this implementation step:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit && composer analyse:phpstan
```

Commit: `refactor(05-01): normalize ip lockout helpers and comments`
  </action>
  <verify>
No behavioral regressions; unit + PHPStan remain green.
  </verify>
  <done>Core code is maintainable and behavior-stable.</done>
</task>

</tasks>

<verification>
- `composer test:unit -- tests/Unit/SudoSessionTest.php --filter "ip|lockout|failed_attempt" --do-not-cache-result`
- `composer test:unit && composer analyse:phpstan`
</verification>

<success_criteria>
- Unit tests enforce IP + user multidimensional lockout policy
- Active IP lockout is enforced in activation flow
- Hook contract is additive and backward-compatible
</success_criteria>

<output>
After completion, create `/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/05-ip-user-multidimensional-rate-limiting/05-01-SUMMARY.md`
</output>
