---
phase: 05-ip-user-multidimensional-rate-limiting
plan: 02
type: tdd
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/Integration/RateLimitingTest.php
  - tests/Integration/ChallengeTest.php
  - tests/Integration/AuditHooksTest.php
  - includes/class-sudo-session.php
autonomous: true

must_haves:
  truths:
    - "Integration tests verify same-IP cross-user threshold behavior"
    - "Active IP lockout blocks subsequent attempts from that source IP"
    - "Audit hook integration captures IP in lockout events"
    - "Single-site and multisite integration behavior remains stable"
  artifacts:
    - path: "tests/Integration/RateLimitingTest.php"
      provides: "Real WP integration coverage for multidimensional lockout"
      contains: "ip"
    - path: "tests/Integration/AuditHooksTest.php"
      provides: "Lockout hook payload coverage including ip"
      contains: "wp_sudo_lockout"
  key_links:
    - from: "RateLimitingTest"
      to: "Sudo_Session IP transient behavior"
      via: "same-IP cross-user attempts"
      pattern: "locked_out"
---

<objective>
Lock in v2.13 behavior at integration level and validate lockout audit payloads.

Purpose: ensure multidimensional rate limiting works with real WordPress persistence and real hook dispatch.
Output: green integration coverage for single-site and multisite.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/ROADMAP.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/05-ip-user-multidimensional-rate-limiting/05-CONTEXT.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/05-ip-user-multidimensional-rate-limiting/05-RESEARCH.md
@/Users/danknauss/Documents/GitHub/wp-sudo/tests/Integration/RateLimitingTest.php
@/Users/danknauss/Documents/GitHub/wp-sudo/tests/Integration/ChallengeTest.php
@/Users/danknauss/Documents/GitHub/wp-sudo/tests/Integration/AuditHooksTest.php
@/Users/danknauss/Documents/GitHub/wp-sudo/includes/class-sudo-session.php
</context>

<tasks>

<task type="tdd">
  <name>Task 1 RED: Add failing integration tests for IP + user combined policy</name>
  <files>tests/Integration/RateLimitingTest.php tests/Integration/AuditHooksTest.php tests/Integration/ChallengeTest.php</files>
  <action>
Add failing integration tests that prove multidimensional behavior.

Required coverage:
1. Same-IP failures distributed across users trigger lockout at threshold.
2. Active IP lockout blocks a correct-password attempt from that same IP.
3. `wp_sudo_lockout` integration callback receives user ID, attempts, and IP.
4. Existing per-user lockout and throttle tests remain behavior-compatible.

Run focused tests (expect RED):
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:integration -- tests/Integration/RateLimitingTest.php --do-not-cache-result
composer test:integration -- tests/Integration/AuditHooksTest.php --do-not-cache-result
composer test:integration -- tests/Integration/ChallengeTest.php --do-not-cache-result
```

Commit: `test(05-02): add failing integration tests for ip user combined lockout`
  </action>
  <verify>
New integration tests fail before production adjustments.
  </verify>
  <done>Integration RED baseline exists for multidimensional policy.</done>
</task>

<task type="tdd">
  <name>Task 2 GREEN: Satisfy integration contracts and edge cases</name>
  <files>includes/class-sudo-session.php tests/Integration/RateLimitingTest.php tests/Integration/AuditHooksTest.php tests/Integration/ChallengeTest.php</files>
  <action>
Implement minimal adjustments needed for integration stability.

Allowed updates:
- edge-case handling in IP lockout remaining/expiry paths,
- deterministic test fixtures for source-IP setup,
- hook callback accepted-args updates where needed.

Run verification after this implementation step:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit && composer analyse:phpstan
```

Then run focused integration verification:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:integration -- tests/Integration/RateLimitingTest.php --do-not-cache-result
composer test:integration -- tests/Integration/AuditHooksTest.php --do-not-cache-result
composer test:integration -- tests/Integration/ChallengeTest.php --do-not-cache-result
WP_MULTISITE=1 composer test:integration -- tests/Integration/RateLimitingTest.php --do-not-cache-result
WP_MULTISITE=1 composer test:integration -- tests/Integration/AuditHooksTest.php --do-not-cache-result
```

Commit: `feat(05-02): enforce multidimensional lockout behavior in integration flows`
  </action>
  <verify>
Targeted single-site and multisite integration tests pass.
  </verify>
  <done>Integration behavior is locked for v2.13 multidimensional policy.</done>
</task>

<task type="tdd">
  <name>Task 3 REFACTOR: Clean integration naming and deterministic fixtures</name>
  <files>tests/Integration/RateLimitingTest.php tests/Integration/AuditHooksTest.php</files>
  <action>
Refactor integration test readability without behavior changes:
- normalize same-IP fixture helpers,
- remove ambiguous naming,
- keep comments aligned with combined-policy semantics.

Run verification after this implementation step:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit && composer analyse:phpstan
```

Commit: `refactor(05-02): tidy multidimensional rate-limit integration fixtures`
  </action>
  <verify>
No regressions in unit or targeted integration coverage.
  </verify>
  <done>Integration tests are deterministic and maintainable.</done>
</task>

</tasks>

<verification>
- `composer test:integration -- tests/Integration/RateLimitingTest.php --do-not-cache-result`
- `composer test:integration -- tests/Integration/AuditHooksTest.php --do-not-cache-result`
- `WP_MULTISITE=1 composer test:integration -- tests/Integration/RateLimitingTest.php --do-not-cache-result`
- `composer test:unit && composer analyse:phpstan`
</verification>

<success_criteria>
- Cross-user same-IP failures trigger lockout correctly
- Hook payload includes IP in integration runtime
- Multisite run has no multidimensional regressions
</success_criteria>

<output>
After completion, create `/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/05-ip-user-multidimensional-rate-limiting/05-02-SUMMARY.md`
</output>
