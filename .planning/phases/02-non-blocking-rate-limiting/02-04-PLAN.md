---
phase: 02-non-blocking-rate-limiting
plan: 04
type: tdd
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - includes/class-challenge.php
  - admin/js/wp-sudo-challenge.js
  - tests/Unit/ChallengeTest.php
  - tests/MANUAL-TESTING.md
autonomous: true

must_haves:
  truths:
    - "AJAX auth error payload forwards delay when attempt_activation returns invalid_password with delay"
    - "Challenge JS disables password submit during throttle windows and shows a countdown/status message"
    - "Throttle UX is distinct from full lockout UX and does not change existing lockout behavior"
    - "When throttle countdown expires, form re-enables and user can retry"
  artifacts:
    - path: "includes/class-challenge.php"
      provides: "delay-aware invalid_password AJAX response payload"
      contains: "delay"
    - path: "admin/js/wp-sudo-challenge.js"
      provides: "client-side throttle countdown/disable behavior"
      contains: "startThrottleCountdown"
    - path: "tests/Unit/ChallengeTest.php"
      provides: "unit coverage for delay forwarding and localized throttle strings"
      contains: "test_handle_ajax_auth_returns_delay_in_invalid_password_response"
  key_links:
    - from: "Sudo_Session::attempt_activation()"
      to: "Challenge::handle_ajax_auth()"
      via: "delay metadata propagation"
      pattern: "invalid_password"
    - from: "Challenge::enqueue_assets()"
      to: "wp-sudo-challenge.js"
      via: "localized throttle strings"
      pattern: "strings"
---

<objective>
Close the Phase 2 UX gap by wiring non-blocking throttle delay through challenge AJAX responses and into a brief client-side submit disable/countdown.

Purpose: keep non-blocking backend security behavior while giving users explicit feedback during throttle windows.
Output: delay-aware challenge AJAX + JS countdown behavior + unit/manual coverage.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/PROJECT.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/ROADMAP.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/02-non-blocking-rate-limiting/02-CONTEXT.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/02-non-blocking-rate-limiting/02-RESEARCH.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/02-non-blocking-rate-limiting/02-01-SUMMARY.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/02-non-blocking-rate-limiting/02-02-SUMMARY.md
@/Users/danknauss/Documents/GitHub/wp-sudo/includes/class-challenge.php
@/Users/danknauss/Documents/GitHub/wp-sudo/admin/js/wp-sudo-challenge.js
@/Users/danknauss/Documents/GitHub/wp-sudo/tests/Unit/ChallengeTest.php
@/Users/danknauss/Documents/GitHub/wp-sudo/tests/MANUAL-TESTING.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 RED: Add failing tests for delay propagation and throttle UX localization</name>
  <files>tests/Unit/ChallengeTest.php</files>
  <action>
Add failing tests that define throttle UX contract before implementation.

Required test coverage:
1. `handle_ajax_auth()` default invalid-password path includes `delay` in `wp_send_json_error` payload when backend result has `delay`.
2. `handle_ajax_auth()` still returns 401 and existing error message contract when `delay` is absent.
3. `enqueue_assets()` localizes throttle-specific strings required by JS countdown behavior.

Run focused tests (expect RED):
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
./vendor/bin/phpunit tests/Unit/ChallengeTest.php --filter "delay|throttle|enqueue_assets" --do-not-cache-result
composer analyse:phpstan
```

Commit: `test(02-04): add failing challenge tests for throttle delay ux wiring`
  </action>
  <verify>
New challenge unit tests fail before production changes.
  </verify>
  <done>RED state established for delay passthrough and localized throttle strings.</done>
</task>

<task type="tdd">
  <name>Task 2 GREEN: Wire delay through AJAX and implement client-side throttle countdown</name>
  <files>includes/class-challenge.php admin/js/wp-sudo-challenge.js</files>
  <action>
Implement minimum code changes to satisfy Task 1.

Implementation requirements:
1. In `handle_ajax_auth()`, when result code is `invalid_password` with `delay > 0`, include delay in JSON error payload.
2. In `wp-sudo-challenge.js`, add throttle handler:
   - detect delay-bearing invalid-password responses,
   - disable submit button for the delay window,
   - show short countdown/status text,
   - re-enable submit when countdown ends.
3. Preserve existing lockout countdown behavior and response handling.
4. Keep accessibility behavior consistent (announce key state changes, avoid noisy per-second assertive announcements).

Run verification:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
./vendor/bin/phpunit tests/Unit/ChallengeTest.php --do-not-cache-result
composer analyse:phpstan
```

Commit: `feat(02-04): implement challenge ajax delay passthrough and throttle countdown`
  </action>
  <verify>
Challenge unit tests pass and no static-analysis regressions are introduced.
  </verify>
  <done>Throttle delay is visible and enforced in challenge AJAX/JS flow.</done>
</task>

<task type="tdd">
  <name>Task 3 REFACTOR: Add manual verification steps and tidy UX semantics</name>
  <files>admin/js/wp-sudo-challenge.js tests/MANUAL-TESTING.md</files>
  <action>
Refine UX text and document manual verification path.

Required updates:
1. Add a concise manual-testing subsection for "Throttle window feedback" to `tests/MANUAL-TESTING.md` challenge coverage.
2. Ensure JS function names/comments distinguish throttle countdown from lockout countdown.
3. Keep error-message semantics clear: throttled state should not appear as repeated generic incorrect-password without wait guidance.

Run verification:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
./vendor/bin/phpunit tests/Unit/ChallengeTest.php --do-not-cache-result
composer analyse:phpstan
composer lint
```

Commit: `refactor(02-04): document and polish challenge throttle ux behavior`
  </action>
  <verify>
Manual guide includes throttle verification steps and code comments remain accurate.
  </verify>
  <done>Throttle UX behavior is documented and maintainable.</done>
</task>

</tasks>

<verification>
- `./vendor/bin/phpunit tests/Unit/ChallengeTest.php --do-not-cache-result`
- `composer analyse:phpstan`
- `composer lint`
</verification>

<success_criteria>
- Delay metadata is propagated from backend to challenge AJAX response
- Client disables retries during throttle window with visible countdown feedback
- Existing lockout behavior remains unchanged
</success_criteria>

<output>
After completion, create `/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/02-non-blocking-rate-limiting/02-04-SUMMARY.md`
</output>
