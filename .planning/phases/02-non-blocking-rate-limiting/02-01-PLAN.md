---
phase: 02-non-blocking-rate-limiting
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/Unit/SudoSessionTest.php
  - includes/class-sudo-session.php
autonomous: true

must_haves:
  truths:
    - "Auth failure path contains no blocking sleep/usleep calls"
    - "Failed attempts are tracked with append rows written via add_user_meta()"
    - "attempt_activation() preserves response-code contract and returns non-blocking delay metadata"
    - "wp_sudo_reauth_failed and wp_sudo_lockout hook contracts remain intact"
    - "Successful activation and expired lockout reset all rate-limit state"
  artifacts:
    - path: "includes/class-sudo-session.php"
      provides: "Non-blocking throttle + add_user_meta append-event failure tracking"
      contains: "record_failed_attempt"
    - path: "tests/Unit/SudoSessionTest.php"
      provides: "Unit coverage for throttle, lockout, and hook behavior"
      contains: "test_attempt_activation_returns_invalid_password_with_delay_when_throttled"
  key_links:
    - from: "Sudo_Session::record_failed_attempt()"
      to: "WordPress metadata API"
      via: "append failure rows using add_user_meta"
      pattern: "add_user_meta"
    - from: "Sudo_Session::attempt_activation()"
      to: "throttle remaining helper"
      via: "short-circuit invalid_password response with delay"
      pattern: "delay"
---

<objective>
Implement the core Phase 2 hardening in `Sudo_Session` using TDD: remove blocking delay calls and replace scalar-counter dependence with append-row failure tracking.

Purpose: eliminate PHP worker blocking and narrow TOCTOU race risk without changing external challenge response codes.
Output: updated rate-limit engine + passing unit coverage.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/PROJECT.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/ROADMAP.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/02-non-blocking-rate-limiting/02-CONTEXT.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/02-non-blocking-rate-limiting/02-RESEARCH.md
@/Users/danknauss/Documents/GitHub/wp-sudo/includes/class-sudo-session.php
@/Users/danknauss/Documents/GitHub/wp-sudo/tests/Unit/SudoSessionTest.php
</context>

<tasks>

<task type="tdd">
  <name>Task 1 RED: Add failing unit tests for non-blocking throttle and append-row tracking</name>
  <files>tests/Unit/SudoSessionTest.php</files>
  <action>
Add failing unit tests that lock in target behavior before production changes.

Required new tests:
1. `test_attempt_activation_returns_invalid_password_with_delay_when_throttled()`
2. `test_attempt_activation_throttle_path_skips_wp_check_password()`
3. `test_record_failed_attempt_uses_add_user_meta_for_append_tracking()`
4. `test_lockout_still_triggers_at_five_attempts_with_non_blocking_model()`
5. `test_successful_activation_clears_throttle_and_failure_event_meta()`

Test intent:
- Throttle path returns quickly with `invalid_password` + `delay` and no blocking call.
- Password hash check is skipped while throttle is active.
- Failure tracking is append-row based (`add_user_meta`), not scalar increment overwrite.
- Threshold/lockout contract remains unchanged.

Run focused tests and confirm they fail against current code:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
./vendor/bin/phpunit tests/Unit/SudoSessionTest.php --filter "throttle|lockout|failed_attempt|add_user_meta" --do-not-cache-result
composer analyse:phpstan
```

Commit: `test(02-01): add failing unit tests for non-blocking append-row rate limiting`
  </action>
  <verify>
Focused unit run shows failures for new tests. Existing unrelated tests remain green.
  </verify>
  <done>New unit tests exist and fail for expected reasons before implementation.</done>
</task>

<task type="tdd">
  <name>Task 2 GREEN: Implement non-blocking throttle and add_user_meta append tracking</name>
  <files>includes/class-sudo-session.php</files>
  <action>
Implement the smallest production change-set that satisfies Task 1 tests.

Implementation requirements:
1. Remove blocking delay calls from auth-failure path (no `sleep()`/`usleep()`).
2. Add time-based retry metadata (throttle-until semantics) and remaining-delay helper.
3. Track failed attempts using append rows (`add_user_meta`) and derived counts from `get_user_meta(..., false)`.
4. Keep response codes unchanged; keep optional `delay` response metadata.
5. Preserve hook behavior:
   - `wp_sudo_reauth_failed` fires on real failed-password processing with derived attempt count.
   - `wp_sudo_lockout` fires once threshold is reached.
6. Ensure resets clear lockout/throttle/failure-event state on:
   - successful activation
   - expired lockout cleanup

Run verification after implementation:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
./vendor/bin/phpunit tests/Unit/SudoSessionTest.php --do-not-cache-result
composer analyse:phpstan
```

Commit: `feat(02-01): implement add_user_meta-based non-blocking rate limiting`
  </action>
  <verify>
All `SudoSessionTest` unit tests pass and PHPStan remains clean.
  </verify>
  <done>Core rate-limit engine is non-blocking and test-covered at unit level.</done>
</task>

<task type="tdd">
  <name>Task 3 REFACTOR: Normalize helpers, comments, and constants</name>
  <files>includes/class-sudo-session.php tests/Unit/SudoSessionTest.php</files>
  <action>
Refactor for clarity without behavior changes:
- Consolidate repeated time/meta logic into private helpers.
- Update PHPDoc/comments to describe add-user-meta append model.
- Keep constant naming and visibility consistent with existing class style.

Run final checks:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
./vendor/bin/phpunit tests/Unit/SudoSessionTest.php --do-not-cache-result
composer analyse:phpstan
```

Commit: `refactor(02-01): clean up append-row non-blocking rate-limit helpers`
  </action>
  <verify>
Refactor introduces no behavior regressions and no static-analysis issues.
  </verify>
  <done>Code is maintainable, documented, and behavior-stable after refactor.</done>
</task>

</tasks>

<verification>
- `tests/Unit/SudoSessionTest.php` passes fully
- No `sleep()` calls remain in `includes/class-sudo-session.php`
- Failure-event writes use `add_user_meta` append semantics
- `composer analyse:phpstan` passes
</verification>

<success_criteria>
- Blocking request-thread delay is fully removed
- Non-blocking throttle semantics are implemented and unit-tested
- TOCTOU susceptibility is narrowed via append-row tracking
- Lockout/audit hook contracts are preserved
</success_criteria>

<output>
After completion, create `/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/02-non-blocking-rate-limiting/02-01-SUMMARY.md`
</output>
